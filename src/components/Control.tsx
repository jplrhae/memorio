export default function Control() {
  return (
    <div
      style={{ display: "flex", justifyContent: "space-evenly", marginTop: 10 }}
    >
      <button>Add partition</button>
      <button>Add process</button>
    </div>
  );
}
