export default function Header() {
  return (
    <div
      style={{
        backgroundColor: "black",
        color: "white",
        padding: 15,
        display: "flex",
        justifyContent: "center",
        fontSize: 30,
        fontWeight: "bold",
      }}
    >
      Memorio
    </div>
  );
}
